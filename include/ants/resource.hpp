#ifndef ANTS_RESOURCE_HPP
#define ANTS_RESOURCE_HPP

namespace ants {

/**
 * A Resource represents an element that an Ant can use or consume.
 */
enum class Resource {
  /// Food
  kFood,
};

} // namespace ants

#endif // ANTS_RESOURCE_HPP
